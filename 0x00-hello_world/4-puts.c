#include <stdio.h>

/**
 * main - program that uses puts function to output data
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
puts("\"Programming is like building a multilingual puzzle");
return (0);
}
