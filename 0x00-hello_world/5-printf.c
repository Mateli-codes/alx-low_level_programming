#include <stdio.h>

/**
 * main - program that print a line using the printf function
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
write("with proper grammar, but the outcome is a piece of art", 93);
return (0);
}
